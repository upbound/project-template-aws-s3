apiVersion: platform.example.com/v1alpha1
kind: XStorageBucket
metadata:
  name: example-python
spec:
  compositionSelector:
    matchLabels:
      language: python
  parameters:
    region: us-west-1
    versioning: true
    acl: public-read
